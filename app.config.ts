export default defineAppConfig({
    ui: {
        primary: 'kitanik-purple',
        defaults: {
            UButton: {
                class: 'rounded-none', // Убираем скругления у кнопок по умолчанию
            },
        },
    },

});
